- name: Install and start nginx
  hosts: webservers
  become: yes   # run as root (like sudo)

  tasks:
    - name: Install nginx
      apt:
        name: nginx
        state: present
        update_cache: yes

    - name: Start and enable nginx
      service:
        name: nginx
        state: started
        enabled: yes
